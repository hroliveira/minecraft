apiVersion: batch/v1
kind: CronJob
metadata:
  name: minecraft-backup
spec:
  schedule: "0 2 * * *"  # Executar todos os dias às 2h
  jobTemplate:
    spec:
      template:
        spec:
          activeDeadlineSeconds: 600
          restartPolicy: OnFailure  # Reiniciar apenas se falhar
          containers:
          - name: backup
            image: ubuntu:latest
            command: ["/bin/sh", "-c"]
            args:
              - |
                mkdir -p /backups && \
                tar -czf /backups/minecraft-backup-$(date +\%F).tar.gz /data && \
                echo "Backup completed."
            volumeMounts:
            - name: minecraft-v2-data  # Nome do volume correto
              mountPath: /data  # Diretório de dados do Minecraft
            - name: backup-storage
              mountPath: /backups  # Diretório onde o backup será armazenado
          volumes:
          - name: minecraft-v2-data
            persistentVolumeClaim:
              claimName: minecraft-v2-pvc  # PVC correto para os dados do Minecraft
          - name: backup-storage
            persistentVolumeClaim:
              claimName: backup-pvc  # PVC para armazenar os backups
